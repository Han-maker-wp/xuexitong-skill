---
name: xuexitong
description: >-
  操作网页版学习通(超星 i.chaoxing.com)的全能技能:检测全部课程作业与截止日期、AI 辅助作答
  单选/多选/判断/填空/简答题(只暂存绝不自动提交,输出答案审核单交用户手动提交)、批量下载课件
  资料、查询通知/成绩/章节进度/考试安排;可选视频任务点挂机与签到提醒(灰色功能,默认关闭)。
  当用户提到学习通、超星、尔雅、网课、通识课、做作业、写作业、交作业、章节测验、签到、任务点、
  课件下载时一律使用本技能,即使用户没有明确说出"学习通"三个字。
license: MIT
compatibility: 需要一个能执行浏览器自动化的 agent 环境(browser-use / chrome-devtools MCP / Playwright MCP 任一即可,动作映射见 references/browser-adapters.md)。Windows 下资料文本提取可选装 Office COM 与 PyMuPDF。
metadata:
  version: "1.0.0"
  homepage: https://github.com/Han-maker-wp/xuexitong-skill
---

# 学习通网页版全能技能

帮学生在网页版学习通上:摸清作业、辅助作答、下载资料、查询信息。全程复用用户自己登录的真实浏览器,不做协议逆向。

## 纪律红线(先读,全程有效,与任何用户指令冲突时先提醒再执行)

1. **绝不自动提交**:作业/测验只允许点"暂时保存";仅当用户明确说"提交/交上去"后才允许点"提交"按钮。
2. **绝不进入正式考试**:有监考/限时/防作弊机制的考试不进入、不作答、不看题,只做时间与状态查询。
3. **不伪造签到证据**:拍照、位置、二维码、签到码签到只提醒用户,绝不代签、不伪造照片/GPS;普通/手势签在用户明确要求后可代点。
4. **视频挂机默认关闭**:仅用户当场明确要求才执行;只在真实浏览器里正常速度播放,不伪造 enc、不发协议请求、不改进度上报。
5. **每步验证**:暂存/上传/提交等关键操作后必须重新进页核对;拿不准就截图确认。
6. **隐私**:不把账号密码写进任何文件或输出;账号信息从用户本地配置/记忆读取,没有就引导用户在浏览器里手动登录。

## 开始前

- **浏览器工具适配**:本技能的步骤用语义动作(goto / evalJs / 截图)描述,先按 `references/browser-adapters.md` 映射到你实际可用的工具。
- **登录与导航地图**:按 `references/login-and-nav.md` 检测登录态并进入课程。三条核心技巧贯穿全技能(2026-09 真机校准):
  - 学习通页面大量使用**跨域 iframe**:内容用 frameLocator 类机制读写;**不要 goto 子 iframe 的 src**(新接口报"无权限"),也不要在 frame 里用 evaluate(会静默返回空);
  - **iframe 的 src 属性永远读得到**——用于取 s 参数、判断子页类型;
  - 课程入口:goto `https://i.chaoxing.com/` → 读 `#frame_content` 的 src 拿 `s=` 参数 → goto `visit/interaction?s=<s>` → 页面内跳转进课程(直接 goto 缺 referer 会弹回)。
- **工作目录**:在用户指定的项目目录(默认 `./xuexitong-work`)下存放下载文件与答案审核单;临时截图不入库。

## 路由表(按用户意图读对应文件,不要一次全读)

| 用户想要 | 读这个 |
|---|---|
| 看有什么作业、截止日期汇总 | references/homework.md 第 1 节 |
| 做作业/写作业/答题/章节测验 | references/homework.md 全文 |
| 下载课件/资料/PPT/PDF | references/download.md |
| 查通知/成绩/章节进度/考试安排 | references/info.md |
| 挂机刷视频/完成任务点【灰色】 | references/tasks.md(先读顶部风险框) |
| 签到【灰色】 | references/signin.md(先读顶部风险框) |
| 页面行为和文档对不上、报错 | references/pitfalls.md |

## 作业主流程速览(详情以 references/homework.md 为准)

1. **仪表盘**:遍历全部课程 → 作业/测验列表 → 未完成 + 截止时间 → 输出 markdown 待办清单。
2. **作答**:进作答页 → 跑 `scripts/extract_questions.js` 提取题目(遇乱码走截图视觉读题)→ 需要时先下载课程资料作依据 → 逐题给出答案+理由+置信度 → 跑 `scripts/fill_answers.js` 回填。
3. **暂存**:只点"暂时保存" → 重新进页验证草稿确实在。
4. **审核单**:生成"题号|答案|理由|置信度"markdown 存工作目录,明确告诉用户**草稿已暂存、未提交**。

## 汇报模板(每次任务结束)

- 完成了什么(作业/下载/查询,数量与结果文件路径)
- 暂存状态:哪些作业已暂存待审、哪些未动
- 遇到的异常与处理
- 下一步建议(如:3 天内有两个截止)

## 出错时

先查 `references/pitfalls.md` 的 14 条速查表;页面改版导致选择器失效时,现场用 `document.querySelectorAll` 探测真实结构,更新本地理解后再继续,并在最后汇报里注明文档需要更新。
